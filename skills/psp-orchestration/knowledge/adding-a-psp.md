# Adding a PSP

This guide walks through creating a mapping file for a new payment service
provider. You can use the `/generate-mapping-for-psp` agent skill.

```
/generate-mapping-for-psp stripe
```

The skill handles research, schema validation, and field mapping automatically.
See `../agent-skills/generate-mapping-for-psp/skill.md` for the full workflow.
