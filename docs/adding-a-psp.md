# Adding a PSP

This guide walks through creating a mapping file for a new payment service provider. You can use the `/generate-mapping-for-psp` Claude Code skill.

```
/generate-mapping-for-psp stripe
```

The skill handles research, schema validation, and field mapping automatically. See below for what it does under the hood.